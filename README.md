# Arduino-LED-Blinking-Program
This Arduino program blinks an LED connected to pin 3. The setup() function sets pin 3 as an output pin. In the loop() function, the LED is turned ON using digitalWrite(HIGH) and stays on for 1 second using delay(1000). Then the LED is turned OFF using digitalWrite(LOW) for 1 second. This cycle repeats continuously, making the LED blink.
